/** The public API: what the TypeSpec compiler loads, and the options type for a consumer's tooling. */
export { $onEmit } from "./emitter.js";
export { $lib, EmitterOptionsSchema, type EmitterOptions } from "./lib.js";
